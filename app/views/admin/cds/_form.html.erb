<%= form_for @cd do |f| %>
    <div class="field row">
      <%= f.label :アーティストID %><br>
      <%= f.number_field :artist_id, class: "col-xs-12" %>
    </div>

    <div class="field row">
      <label>ジャケット</label><br>
      <%= f.attachment_field :image, placeholder: "ジャケット写真", class: "col-xs-12" %>
    </div>

    <div class="field row">
      <%= f.label :タイトル %><br>
      <%= f.text_field :title, class: "col-xs-12" %>
    </div>

    <div class="field row">
      <%= f.label :値段 %><br>
      <%= f.text_field :price, class: "col-xs-12" %>
    </div>

    <div class="field row">
      <%= f.label :ジャンル %><br>
      <%= f.select :genre, Cd.genres.keys.to_a, {}, class: "col-xs-12" %>
    </div>

    <div class="field row">
      <%= f.label :在庫 %><br>
      <%= f.text_area :stock, class: "col-xs-12" %>
    </div>

    <div class="field row">
      <%= f.label :リリース日 %><br>
      <input type="date" name="cd[release]" id="cd_release" class= "col-xs-12" >
    </div>


  <h3>Disk</h3>
  <div id="discs">
    <%= f.fields_for :discs do |disc| %>
      <%= render 'disc_fields', f: disc %>
    <% end %>
    <div id="links">

      <%= link_to_add_association 'ディスク追加',f, :discs %>
    </div>
    <div class="actions row">
          <%= f.submit ' 追加 ', class: "btn col-xs-12"%>
    </div>
  </div>
<% end %>
