<%= form_tag cd_search_path, :method => 'get' do %> <%#projects_pathはモデル名で読みかえること %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>



<p>CD一覧</p>


	<% @cds.each do |cd| %>
       <%= cd.title %>

  <% end %>


<%= link_to "ログアウト", destroy_user_session_path, method: :delete, data: {confirm: 'go to hell?'} %>